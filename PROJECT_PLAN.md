# RCM Ticket Recommendation System — Project Plan

Living document. Updated as decisions are made. This is the source of truth for project state (memory holds only the high-level summary).

## Problem

Clients email an RCM organization about existing or new issues. Account Managers manually decide: new ticket / attach to existing ticket / reply / archive. Follow-up emails about existing issues are often misclassified as new tickets, creating duplicates and wasting manager time.

## Scope (deliberately narrow)

Build a **recommendation system**, not an automation system:

> Given a new incoming email, recommend the most likely existing ticket it belongs to, with a confidence score. Manager approves or rejects. No auto-create, auto-reply, or auto-archive logic.

Pipeline (refined): Incoming email → Identify customer (deterministic lookup, not ML) → Fetch that customer's OPEN tickets → Take each ticket's N most recent message interactions (N configurable, default 10) → Embed incoming email → Compare against that filtered message-level embedding pool → Top-K similar messages → Group by ticket_id → Ticket score = MAX similarity among its retrieved messages (baseline; scoring strategy pluggable) → Recommend highest-scoring ticket + score → Manager decision.

## Environment

- Windows 11, VS Code, Python, FastAPI, PostgreSQL
- Local GPU: RTX 3050, 4GB VRAM — fine for inference with small/medium embedding models; likely insufficient for fine-tuning larger models or large-batch contrastive training → use Google Colab for those steps (flagged per-phase below).
- No historical email data available → synthetic dataset required (Phase 1–2).

## Roadmap & Status

| Phase | Title | Status |
|---|---|---|
| 1 | Dataset Design | **In Progress** (spec ✅, generation prompt templates ✅, QA checklist ✅, execution roadmap ✅, schema code realigned to production ✅, manual pilot generated + QA'd ✅; **DB not yet re-migrated** — `app/enums.py`/`app/models.py` have moved past the original `817ec074e0f5` migration, still pending an Alembic revision before anything gets inserted into Postgres) |
| 2 | Synthetic Dataset Generation | Not started |
| 3 | Embedding Model Selection | Not started |
| 4 | Vector Database | Not started |
| 5 | Similarity Search | Not started |
| 6 | Evaluation | Not started |
| 7 | Embedding Model Comparison | Not started |
| 8 | Fine-tuning | Not started |
| 9 | FastAPI Integration | Not started |
| 10 | Production Architecture | Not started |

## Decisions Log

_Format: Phase — Decision — Rationale — Date_

- **Phase 1 — Embedding unit = message-level, aggregated to ticket (Approach B)**, not one-vector-per-ticket and not an evolving summary vector. Rationale: follow-ups usually mirror one specific prior message, not a thread average; message-level preserves that signal and is explainable (can show manager *which* prior email matched). Evolving-summary approach deferred to Phase 10 as a possible later evolution. — 2026-07-22
- **Phase 1 — Search scope = tenant-filtered**: identify customer from incoming email (deterministic, non-ML lookup) → restrict candidate pool to that customer's OPEN tickets only → within each ticket, most recent `MAX_RECENT_INTERACTIONS` messages (default 10, configurable, not hardcoded). Closed tickets excluded from baseline scope (extend later). — 2026-07-22
- **Phase 1 — Ticket scoring = pluggable strategy interface; baseline implementation = MAX similarity.** Confirmed: do not lock in MAX. Strategies to compare later: Max, Average, Weighted Average, Top-k Voting, Reciprocal Rank Fusion. This comparison is itself a research deliverable of **Phase 6 (Evaluation)** — needs the labeled eval set (Phase 1 labeling strategy) to score each strategy against. — 2026-07-22
- **Phase 1 — Sender scope confirmed**: store `sender_type` (client/manager) on every message; baseline comparison pool = client-authored messages only, filterable via config. — 2026-07-22
- **Environment strategy**: VS Code + local RTX 3050 (4GB) is primary/default for all phases except fine-tuning. **Google Colab reserved specifically for Phase 8 (Fine-tuning)**, and optionally for benchmarking larger embedding models in Phase 7 if local VRAM is too tight. Rationale: embedding *inference* (small sentence-embedding models) is cheap and fits comfortably in 4GB; contrastive fine-tuning needs larger batch sizes (more in-batch negatives → better gradient signal) which 4GB can't comfortably give. Colab is treated as an ephemeral training environment only — trained artifacts get downloaded and served locally, never served from Colab itself. — 2026-07-22
- **Phase 1 — Benchmark dataset spec finalized**, written up at `docs/benchmark_dataset_spec.md` (scale, category/intent distributions, 6 difficulty tiers, style variation, ground-truth schema). Ticket corpus and eval-query set treated as two distinct generation targets. — 2026-07-23
- **Phase 1 — Schema implemented** in `app/` (SQLAlchemy 2.0 + Alembic, Postgres via psycopg2): `Customer`, `CustomerEmail` (normalized sender→customer lookup table, unique on `email_address`), `Ticket` (composite index on `customer_id, status` — the pipeline's core filter), `Message` (index on `ticket_id, created_at` for the "most recent N" query; includes reserved-but-unused `claim_number`/`patient_id`/`payer`/`date_of_service`), `EvalQuery` (separate table for benchmark ground truth, not part of the production model). No embedding column yet — deferred to Phase 3/4. Shared `app/enums.py` mirrors the category/intent/difficulty/style vocab from the benchmark spec so generation code and schema can't drift apart. — 2026-07-23
- **Phase 1 — Generation workflow = hybrid, prompts-first**: design the 5 generation prompt templates (customer, ticket seed, conversation, eval query, ground-truth label) → hand-run them through an LLM to produce a small manual pilot (~20–30 tickets, no API pipeline yet) → review realism/diversity/labels → freeze prompts → only then build the automated batch-generation pipeline. Rationale: validates the actual prompts before investing in automation, rather than automating first and discovering prompt problems at scale. Templates written up at `docs/generation_prompts.md`; ground-truth labeling is a deliberately separate *blind* pass (Template 5 doesn't see Template 4's intended answer) so label agreement rate becomes a free QA signal on top of the spec's existing 10% manual spot-check. — 2026-07-23
- **Phase 1 — Automated Generation QA gate designed**, written up at `docs/generation_qa_checklist.md`: every generated item gets a PASS/FLAG/FAIL verdict from a mix of deterministic rule checks (enum validity, referential integrity, leakage-scan, structural ordering), one batch-level statistical check (realized distributions vs. spec targets), and two new LLM judges (ticket/conversation consistency; distractor realism) — deliberately not nine separate judge calls, since most checks are cheaper and more reliable as plain code, and "does ground truth match"/"is the difficulty tier satisfied" reuse the existing Template 5 blind-judge mismatch rather than re-litigating it. FAILs auto-regenerate; FLAGs go to the front of the manual review queue, turning the spec's flat 10% spot-check into a risk-weighted one. — 2026-07-23
- **Phase 1 — Realigned taxonomy + docs to the finalized business workflow.** `docs/benchmark_dataset_spec.md` and `docs/generation_prompts.md` now open with the real AM-first process (informational/general-query emails never become tickets; most ticket follow-ups arrive via automatic email threading with no AI involved; retrieval is only invoked for a new independent email that broke threading and must be checked against OPEN tickets). `TicketCategory` reframed as operational-team ownership (Claims, Payment Posting, Prior Authorization, Accounts Receivable, Eligibility, Charge Entry, weighted 25/18/16/15/14/12) with a new nested **Issue Type** concept (closed enum per category, 8–11 values, default v1 vocabulary — extensible later without changing the template/QA structure). `hard_negative` and `same_customer_disambiguation` tiers kept exactly as designed, just reframed/clarified: hard_negative measures "correctly find no OPEN match" within the in-scope retrieval decision (not a ticket-creation judgment); disambiguation siblings now require same category **and semantically similar** (not identical) issue types, since category alone is now too coarse to guarantee difficulty. Old categories `documentation_request`/`new_service_request`/`insurance_verification`/`general_enquiry` retired — folded into issue types or dropped (general_enquiry → no ticket at all, per the new Case 1). **Follow-up debt**: `docs/generation_qa_checklist.md` realigned to the new taxonomy (2026-07-24, see next entry) — remaining debt is `app/enums.py`/`app/models.py`, which still need a real `IssueType` enum, updated `TicketCategory` values, and an `EvalQuery` field capturing the attach/no-match business-action distinction. — 2026-07-24
- **Phase 1 — `docs/generation_qa_checklist.md` realigned** to the Category/Issue Type taxonomy: category enum list updated to the 6 operational teams; new rule check for issue_type validity; sibling-distinctness check split into a mechanical rule (facts differ) plus a new semantic **Judge 1** question ("sibling pairing plausibility" — same category, similar-not-identical issue type, e.g. claim_denial vs. claim_rejection_clearinghouse is plausible, claim_denial vs. documentation_request_from_payer is not); Judge 1's category-fit question extended to category-*and*-issue_type fit; `hard_negative`/disambiguation distractor rules updated to the issue_type-aware definitions; batch-level statistical check now tallies `issue_type` too. QA architecture unchanged — still 2 judges, same PASS/FLAG/FAIL gate, same rule-vs-judge division of labor. — 2026-07-24
- **Phase 1 — Execution roadmap written** at `docs/execution_roadmap.md`: 9 concrete phases from DB/schema prep through pilot generation, pilot QA, prompt freeze, automated pipeline build, full-scale generation, full-scale QA, to benchmark freeze — each with inputs/outputs/dependencies/success criteria/artifacts. Re-examined and closed one piece of previously-flagged debt: `EvalQuery` does **not** need a new field for the attach/no-match business action — `should_match` + nullable `correct_ticket_id` already encode it. Remaining code debt (new `IssueType` enum, updated `TicketCategory` values, Alembic migration) is now Phase 1 of that roadmap, not yet executed. Two numeric thresholds proposed for the first time (not previously fixed anywhere): pilot ground-truth agreement rate ≥90%, pilot FLAG-rate sanity bound <40% — both adjustable. — 2026-07-24
- **Phase 1 — Schema realigned to the real production ticketing system**, per a (user-supplied, unverified by me — no repo access, and the reference doc was truncated) technical schema reference for the actual `unified-backend/` app this recommendation system must integrate with. Corrected two of my own earlier decisions rather than just applying the user's initial ask in isolation: (1) `IssueType`/`CATEGORY_ISSUE_TYPES` **deleted entirely** from `app/enums.py` — confirmed the real system has no such concept, and it wasn't load-bearing for the retrieval pipeline (retrieval only ever consumes message-text embeddings) nor for anything the schema couldn't already do via free-text `distinguishing_details`; (2) `TicketCategory` expanded to 7 values (added `PATIENT_CALLING`, flagged open re: email-volume relevance since it may be phone-originated) with member *values* kept as our own readable vocabulary rather than production's abbreviated strings (`PA`/`AR`) — added a `PRODUCTION_CATEGORY_LABEL` translation dict instead, so the mapping lives at one boundary rather than propagating abbreviations through every prompt/doc. `TicketStatus` expanded from 2 to the real 6 values (`OPEN/IN_PROGRESS/PENDING/WAITING_FOR_CLIENT/RESOLVED/CLOSED`) — the pipeline's "open ticket" scope is now `TERMINAL_TICKET_STATUSES`-based (non-terminal), not literally `status == OPEN`, since the literal reading would have wrongly excluded live tickets in the 4 intermediate states. `Message.intent_type`/`claim_number`/`patient_id`/`payer`/`date_of_service` removed as **persisted columns** — none exist in the real system either (no matching field, or only inside an unstructured payload blob) — but `MessageIntent` etc. remain valid generation-time-only concepts (used to shape realistic synthetic threads, just never queried as DB columns). Documentation debt (all 4 docs still describe the pre-correction taxonomy) flagged, not yet done. — 2026-07-25
- **Phase 1 — Pilot dataset generated and QA-validated**: 5 customers, 25 tickets, 128 messages, 20 eval queries, 20 blind-judge labels, all under `pilot/` (manifest, raw generation output per template, QA report). Generated and blind-judged by the same model/session — a real, flagged limitation relative to the design's intent of independent generation vs. judging (Phase 6's automated pipeline should use a genuine separate API call). QA gate caught two real issues before they could compound at scale: (1) two tickets' `closed_at_offset_days` were inconsistent with their own conversation's last message — fixed; (2) the boilerplate difficulty tier's original zero-content construction ("just checking in?") was genuinely unresolvable by a blind judge whenever a customer had multiple open tickets, which dropped ground-truth agreement to 85% (below the proposed ≥90% bar) — traced to one root cause (not scattered noise), fixed by requiring boilerplate queries to retain exactly one topically-discriminating token, which brought agreement to 100%. Recommended as a wording addition to `benchmark_dataset_spec.md`/`generation_prompts.md`'s boilerplate tier definition (not yet applied). — 2026-07-25
- **Phase 1 — `Customer` realigned to production `clients`, same treatment applied to `Ticket`/`Message` earlier.** `CustomerEmail` (multi-contact roster table) removed entirely — the real system has exactly one `inbox_email` per client, no separate known-contacts table; individual staff addresses vary per message without being tracked as a first-class roster. `Customer` gained a real `inbox_email` column. `specialty`/`practice_size`/`primary_payers`/`pm_ehr_system`/individual `contacts` remain generation-only (never persisted) — same bucket as `IssueType`/`MessageIntent`/claim-patient-payer facts. Introduced a structural convention across all corpus-facing raw generation output (`production_fields` / `generation_metadata` nested objects, applied to `pilot/raw/customers.json`, `tickets.json`, `conversations.json` and baked into `generation_prompts.md` Templates 1–3's output schemas) so the separation is enforced by the data's shape, not just prose commentary — an ingestion script can read `production_fields` alone and ignore the rest. Also fully paid down the `issue_type` documentation debt flagged earlier: `generation_prompts.md` and `generation_qa_checklist.md` both still had extensive `issue_type` machinery (Template 2's whole grounding table, Judge 1's category-fit and sibling-plausibility questions, the traceability table) left over from before that field was deleted from the schema — all rewritten to use free-text `core_issue_summary`/`distinguishing_details` instead, consistent with how the pilot data already worked in practice. — 2026-07-25
- **Phase 1 — `patient_calling` resolved: removed from `TicketCategory` entirely**, not merely weighted at 0%. User confirmed this category is slated for removal from production itself, not just irrelevant to an email-driven benchmark — so there's no reason to model it at all, even for schema fidelity. `TicketCategory` is back to 6 values. No pilot data needed regenerating (it was already tested at 0 tickets); only the enum, `PRODUCTION_CATEGORY_LABEL`, `generation_prompts.md`, `generation_qa_checklist.md`, and the pilot manifest/QA-report's mentions of it were updated. — 2026-07-25
- **Phase 1 — Heavy-noise spot-check completed**, `pilot/spot_check_heavy_noise.md`: 3 messages + 3 eval queries spread across `easy`/`moderate_paraphrase`/`hard_semantic` tiers, since the full pilot ended up with zero `heavy`-noise items by sampling accident (not a deliberate exclusion, unlike `patient_calling`). Unlike the boilerplate tier, **no bug found** — the existing prompt wording produces realistic-but-messy text on the first attempt, the QA checklist's noise-level heuristic correctly classifies all 6 as `heavy`, and noise level held independent of difficulty tier (explicit identifiers survived the noise on the `easy` item; the `hard_semantic` item stacked vague-and-noisy without becoming incoherent). No changes made to the noise-level prompt wording or QA rule. Recommendation: proceed to Phase 6 and trust normal sampling to hit the ~10% heavy-noise target at full scale. — 2026-07-25
- **Phase 6 — Automated pipeline built, then re-platformed off the Anthropic API onto local Ollama** (`generation/` package: config, schemas, sampling, state, prompts, QA gate, pipeline, ingest, CLI). Original design used Claude via the Message Batches API; user decided against Anthropic API costs and switched to a local `qwen3:4b` server — `generation/llm_client.py`'s `OllamaClient` replaced `batch_client.py`'s `ClaudeClient` behind the same `call_sync` interface, batch-specific machinery (submit/poll/fetch, `execution_mode`) removed, local `ThreadPoolExecutor` fan-out replaces cloud batching. Live-tested against the real local server: works mechanically end-to-end, but `qwen3:4b` showed inconsistent reliability on the structurally heavier templates (conversations, eval queries) — occasional timeouts/malformed output on a small local model, not an integration bug. A real 40-customer run was started, then stopped by the user (18/40 customers had completed) specifically to revise the dataset design before generating more — resumable via the existing state store, not restarted from scratch. — 2026-07-29
- **Scope change — this project is no longer "one benchmark dataset for one model."** User redefined the goal: build a shared, production-aligned synthetic RCM environment (mirroring the real application's full schema/workflows/operational history) that multiple other automation projects and interns will build on top of, with research artifacts (this project's recommendation benchmark among them) derived from it but kept structurally separate from the operational layer. Before any design, produced a discovery checklist (`docs/production_environment_discovery.md`) covering application scope, full schema, entity lifecycles, workflows/business rules, supporting content, scale/temporal realism, and shared-environment governance (operational/research separation mechanism, other consumers' requirements, disposition of this project's existing benchmark work) — organized into 7 phases for incremental answering. No design decisions made yet; blocked on the user's answers. — 2026-07-29
- **Real production schema received** (`RCM_APPLICATION_KNOWLEDGE_BASE.md`, user-supplied, code-verified against `unified-backend/`/`shared_models` by its own account) — supersedes the earlier truncated schema reference this project had been working from. Full RBAC + ticketing domain: `users/roles/categories/permissions` + `clients/interactions/tickets/attachments/resolution_slas/first_response_slas/ticket_escalations/escalation_handling_slas/sla_policies/notifications/audit logs` (two separate audit tables, two separate Alembic chains, one physical Postgres DB). Produced `docs/application_understanding_report.md` as a comprehension checkpoint (business domain, architecture, entities/relationships/workflows/constraints, operational-vs-reference-vs-derived categorization, ambiguities) before any design work — no design decisions made. Flagged prominently: the real schema is substantially different from this project's existing `app/models.py` (no `Message` table at all — the real `Interaction` is one polymorphic table covering email/reply/note/attachment; `ticket_type` is an unconstrained string in production, not the enum this project built; confirmed no production ground-truth/eval concept, so `EvalQuery` stays correctly understood as a research-only bolt-on). The "disposition of existing work" question from the discovery checklist (Phase 7.3) is now considerably more consequential given how large that gap actually is. — 2026-07-29
- **Project Architecture Specification written** (`docs/project_architecture_specification.md`) — defines the whole-project layer structure before any synthetic-world or generator design: Production Reference (versioned understanding of the real app, not the app itself) → Generation Engine (process) → Synthetic Operational Dataset (one shared artifact) → Research/Derived Artifact Layer (repeats once per consumer project, not a fixed enumerated list) → Consumer Systems (recommendation project is the first instance of a repeatable pattern, not a special case). Data flow reframed as a hub with independent per-consumer spokes, not a linear pipeline ending at the recommendation system — deliberately no feedback edge from consumer output back into the operational dataset (kept acyclic/reproducible for now, revisit only if a specific future consumer needs it). Existing-work assessment: `state.py`/`llm_client.py`/`database.py` unchanged; `config.py`/`sampling.py`/`qa/*.py`/`pipeline.py`/`cli.py`/the 3 generation docs refactored (methodology kept, content rewritten); old `app/models.py` Customer/Ticket/Message + enums, the old Alembic chain, `ingest.py`, and `execution_roadmap.md` replaced; `EvalQuery` + its style/difficulty enums + `pilot/`'s domain-content realism stay research-only/reusable-as-content. **Surfaced a direct factual contradiction**, not yet resolved: this project removed `patient_calling` from `TicketCategory` earlier this session on the basis that production was retiring it, but the new knowledge base lists Patient Calling as a current, fully-detailed production category with no deprecation mentioned — needs reconciling before the schema mirror (D1 below) locks in a category list. Recommended a 7-document sequence before implementation: D1 Production Schema Mirror → D2 Data Governance & Separation → D3 Synthetic World Content/Scale/Temporal Spec → D4 Generation Methodology (LLM-authored vs. simulation-derived per entity — a new distinction, since SLA/escalation/audit/notification rows are business-logic simulation, not prose) → D5 Operational-World QA Framework → D6 Recommendation Benchmark Extraction (where the original benchmark work resumes, now scoped correctly as one consumer's derived artifact) → D7 Generation Pipeline Technical Design (deliberately last). No design decisions made in this document itself. — 2026-07-30

## Open Questions

- **Closed-ticket lookback**: baseline ignores closed tickets entirely (per user decision). Recommend modeling this as `CLOSED_TICKET_LOOKBACK_DAYS = 0` (configurable) rather than a hard exclusion, so "reopen if replied within N days of closing" can be added later without re-architecting. Not yet confirmed.
- **Unknown/unidentified customer fallback**: what happens when the deterministic customer-lookup fails (new sender, forwarded email, shared inbox)? Not yet decided — needs a synthetic-data category in Phase 2 either way.
- ~~**Customer-lookup mechanism needs revisiting**~~ — **RESOLVED 2026-07-25**: sender-based, confirmed by the user against the real production Graph architecture. Every client sends *to* the same shared RCM mailbox (recipient address is identical for everyone, carries zero distinguishing information — my earlier recipient-based recommendation was wrong). Lookup matches the incoming email's **From** address against `Customer.inbox_email`, which represents the client's own recognizable address, not a mailbox we host for them. Corrected in `app/models.py`'s `Customer` docstring and `docs/generation_prompts.md` Template 1. Exact match granularity (exact address vs. domain-level) not specified — not blocking for dataset generation, revisit when Phase 5 (Similarity Search) actually implements the lookup.
- **Vector store for MVP**: since retrieval is pre-filtered to one customer's open-ticket messages (small candidate set, likely dozens–low hundreds), a full ANN vector DB may be unnecessary for the baseline — `pgvector` in the existing Postgres instance could suffice with exact cosine similarity over the filtered set. Revisit properly in Phase 4; noted early since it shapes how "modular vector store" should be abstracted.
- **Confidence score calibration**: raw cosine similarity (e.g. 0.94) is not a calibrated probability. "94% confidence" as shown to managers will need calibration work in Phase 6 (Evaluation) before the number can be trusted at face value.
- **Blocking — production environment discovery**: the scope-change work above is blocked on the user's answers to `docs/production_environment_discovery.md` (application scope, real schema, workflows, scale, and shared-environment governance). No further design/generation work proceeds until at least Phase 1 of that checklist is answered.
